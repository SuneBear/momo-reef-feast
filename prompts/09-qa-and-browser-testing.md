# 浏览器 QA Prompt

```text
请对当前《沫沫的珊瑚午餐》执行完整浏览器 QA，并直接修复发现的问题。先启动本地静态服务器，使用真实浏览器和页面可见状态验证，不要仅做源码静态检查。修改后回归相关路径，直到没有阻断性问题。

加载独立资源版和全内嵌版；检查控制台无 error、所有图片/视频/BGM/SFX 无 404、页面无外网运行时请求。资源请求成功只是基础，还要核对图片/CSS 变量已映射并实际显示、视频 readyState/currentTime 和计算透明度正常、音频已接到对应事件。测试 1280×720 桌面、390×844 竖屏、844×390 低高度横屏、一个中间缩放尺寸和一个高 DPR 尺寸。检查封面、HUD、safe-area、提示、按钮、画布比例、点击区域和文字无裁切/重叠；特别确认英文、数字和百分比不会明显比中文更粗。

逐项实际操作：手动开始、指针/触控目标、WASD/方向键、Shift/按住冲刺、猎物闪躲、吃鱼、连续吃鱼、碰撞受伤、三颗心、短暂无敌、暂停恢复、静音、音量、观赏模式切换与手动接管、100% 无敌、继续成长到 180%、鲨鱼三次攻击、下一关、三关通关、失败和重开。

让观赏模式从第一关至少完整通关一次，观察是否卡边、追逐不可达目标、反复撞危险、100% 后不打鲨鱼或击败后不换关。测试暂停/缩放/切后台后恢复。测试 prefers-reduced-motion，确保非必要动效减少但状态仍清楚。

检查动画 spritesheet 帧序、锚点、翻转和连续移动时的稳定性，不能只看静止帧。让鲨鱼王和玩家达到最大实际绘制尺寸，在标准 DPR 与高 DPR 下截图，确认没有因源分辨率不足或错误图像平滑而模糊；核对验证报告中的源尺寸、放大倍率和 DPR 与运行时一致。把关键透明素材叠在深蓝、白色和高饱和对比底上，检查精确品红、近似紫色和半透明边缘溢色，不能只看透明四角。

检查鱼体俯仰、头灯锚点与光束共用变换；指针贴近玩家、键盘移动、左右翻转和缩放时光束不消失、不跳长、边缘不成硬三角。检查三层视差、焦散、视频后备、角色水下融合、粒子和 UI 图标；背景在底部和侧边无黑块、断层或接缝。

专项核对最终 Glass + Water 混合 UI：半透明效果确实可见，并在不支持 `backdrop-filter` 时仍清楚可读；高质量像素图标、固定装饰和少量 `border-image` 对话框均实际显示。对使用切图的局部继续检查固定边角未缩放、边条只沿单轴伸展、中心安全区充足、动态宽度无接缝/黑边、normal/hover/active 几何一致。

最终界面必须满足：圆润形态没有退化成切角方框；冲刺提示不是突兀的长矩形；图标都显示且语义不重复；成长条/音量等渐变填充可见；文字与图标垂直居中；上下 padding 平衡；核心面板无尴尬空洞；dialog 装饰外无黑色矩形；主按钮 hover 不变形或露出错误颜色。检查 README 中动画、场景、景深、UI 四张 contact sheet 链接真实存在。

实际试听 BGM、普通吃鱼、重型吞食和受伤：普通吃鱼应可爱轻软而不是枪声，受伤必须更明显但不刺耳，BGM 应与较暗的水下氛围一致。不要只验证音频元素能播放。

把发现按严重度记录，但不要止于报告：直接修复 P0/P1/P2，再回归。最终输出简洁测试矩阵，包含尺寸、版本、通过路径、修复项和确实无法解决的限制；不要声称没有实际执行的测试。
```
