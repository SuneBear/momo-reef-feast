# 构建、离线与静态托管 Prompt

```text
请检查当前《沫沫的珊瑚午餐》，专项完善构建、离线运行和静态托管兼容性。直接修改构建脚本、生成产物并验证；保留游戏功能和素材质量。不需要执行 GitHub Pages 发布、创建远程仓库或进行其他远程部署。

生成两种一致版本：项目目录的 index.html 使用 assets/images、assets/audio、assets/media 等相对路径，适合普通静态服务器和任意子路径；另生成 momo-reef-feast.html，把所有图片、视频和音频转成正确 MIME 的 data URL，形成可直接打开的单 HTML。两版必须来自同一个模板和 manifest，不能手工维护两份逻辑。

页面运行时不得请求外网；字体使用本地系统栈；音频、焦散视频、角色、场景、景深和 UI 都在项目内。构建必须把 README.md、prompts/00-one-shot.md、其余 prompts/ 模块文档、音频 CREDITS、workflow/sources、workflow/tools，以及验证 JSON 和四张 contact sheet 一并保留/打包。不要删除用户文档或未相关文件。

构建脚本使用相对路径，不依赖某台机器的绝对目录；缺少必需素材时快速失败并指出文件。输出前清理旧的受控产物，但不得对宽泛目录执行危险删除。manifest 中资源名唯一，HTML 占位标记每个只替换一次。输出构建统计和单 HTML 大小。

启动本地静态服务器验证 index.html；再直接或通过服务器加载内嵌 HTML。检查控制台、网络 404、MIME、视频后备、音频交互解锁、所有关卡和 UI 图标。模拟子路径托管，确保相对 URL 不依赖站点根目录。README 写清本地运行和通用静态托管方式，但不把实际远程发布列为交付条件。完成后列出两种入口、包内文件和验证结果。
```
