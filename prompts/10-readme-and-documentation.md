# README 与 Prompt 文档 Prompt

```text
请检查当前《沫沫的珊瑚午餐》的实际代码、资产、构建产物和验证报告，专项完善文档。文档必须与事实一致；不要只根据旧 README 复述，也不要声称未运行的测试。

README.md 使用简体中文，写清：项目简介；本地运行和通用静态托管；鼠标、触控、键盘、冲刺、暂停、音量；手动与水族馆观赏模式；成长、100% 无敌、180% 上限、鲨鱼三次攻击和三关；美术方向、三层景深、焦散与头灯；最终 Glass + Water 混合 UI——动态 CSS 玻璃容器与独立透明像素图标结合，只有稳定的局部对话框使用 `border-image`；自动化素材流程和工具；验证统计与修复结果；参考素材如何分析或转换；BGM/SFX 的查找、选择、来源和许可；原生 HTML/CSS/Canvas/JS 技术说明。不要求记录或执行 GitHub Pages 实际发布。

增加“实验目的”章节：说明该项目用于测试模型从一条完整 Prompt 出发，自主完成概念、生图、切图、开发、浏览器验证、自动修复、构建和文档的闭环能力；`prompts/`、原始素材、处理脚本和验证报告用于复现与审查。说明未来模型升级后可在干净目录再次运行同一份 00-one-shot，并使用相同 QA 矩阵比较效果。建议记录模型版本、运行日期、环境、人工追加指令次数、完成用时、自动修复轮数、美术一致性、动态 UI 稳定性、玩法完成度和最终限制；不得编造当前没有记录的基线数据。

文档还要如实记录素材流水线的已知限制和后续改进：大型鲨鱼等素材不能统一按普通鱼的 256 px 帧预算处理，应根据最大运行时尺寸、成长倍率和 DPR 提高源分辨率；精确 #ff00ff 匹配会漏掉近似紫色与半透明边缘溢色，需要背景连通的容差色键、edge despill 和对比底复核。若本地脚本仍处理不好，允许使用可免费访问的在线 AI remove-background 工具或外部图像编辑服务；README 要记录所用工具、链接、日期、处理对象、许可/条款和本地化结果，并说明外部输出仍经过细节损失与透明边缘验收。不要在肉眼仍可见模糊或紫边时声称“全部通过”。

增加“当前能力边界与未来路线”说明：本次 Codex 环境没有可直接调用的视频生成能力，因此角色动画由 ImageGen 直接生成 sprite sheet。未来若模型或工具获得可靠视频生成能力，可以先生成固定镜头、固定朝向、纯色背景的短循环角色动画，再定距抽帧、去背、像素化、统一画布和锚点对齐；这种路线通常比独立生成动画帧有更好的时间连续性，也能表现更灵活复杂的动作。文档同时提醒视频路线仍需检查循环首尾、形体/装备漂移、镜头移动、运动模糊和身份一致性，并建议未来模型版本用同一 Prompt 对比两条路线。

在“素材检查”章节直接用 Markdown 显示 workflow/reports/ 下的 animation-contact-sheet.png、scene-contact-sheet.png、depth-contact-sheet.png、ui-contact-sheet.png，每张有简短说明。核对链接、文件名、数量、帧数、锚点误差和 UI 数量与实际 JSON 一致；当前 UI 报告应包含 16 个 atlas 基础素材与 1 个独立成长图标，共 17 项。

仓库根目录保存 `PROMPT.md` 作为最终用户的一键复现入口，并在 `prompts/00-one-shot.md` 保留完全相同的汇总正文；其余 Prompt 集中放在 `prompts/`。汇总 Prompt 覆盖制作代理自动循环、玩法、观赏 AI、美术、场景、UI、音频、流水线、构建、测试和文档；README.md 与 prompts/README.md 分别链接汇总 Prompt、核心玩法、观赏 AI、美术角色、场景景深、UI 字体、音频反馈、素材流水线、构建离线、浏览器 QA、README 文档等可独立复制的模块 Prompt。更新时必须核对根目录和模块目录的汇总版本保持一致。

每份模块 MD 必须包含一个完整代码块，单独发送给 Agent 后能在当前项目完成对应专项；写明保持其他能力、直接修改、运行验证和交付结果。汇总 Prompt 要告诉用户无需继续补充或确认即可完成整款游戏。

检查所有 Markdown 相对链接、本地图片渲染、标题层级、代码块闭合和中英文术语一致性。删除重复、过期或与实现冲突的描述，但保留对复现真正有用的细节。完成后列出更新的文档和已核对的事实来源。
```
